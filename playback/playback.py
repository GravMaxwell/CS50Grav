# Problem ), part 2: Playback Speed - by GravMaxwell :)

# Ask the user why they talk so slow
question = input("Why you talk so slow? ")

# Echo user reponse but replace every space with "..." between words
spacedots = question.replace(" ", "...")

# Print modified user response
print(spacedots)
